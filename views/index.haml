%p.plum.sub-header.small Problema sa Pag-ibig?
%h1 I Ramon Bautista Mo!

%button{class: "btn btn-classic btn-round tenen"} Pindot na. hihihi
%br
%strong Tandaan:
%small There's more to life than love

%p.answer


:javascript
  $(".tenen").click(function(){
    showLoading();
    $.get("/proxy.json", function(data){
      $(".answer").html("<h3>"+data.title+"</h3><p.plum>"+data.content+"</p>");
    });
  });

  showLoading = function(){
    $(".answer").html("<i class='fa fa-refresh fa-spin fa-lg'></i>");
  }

